import SwiftUI

struct TradeTicketView: View {
    var body: some View {
        ContentUnavailableView("Trade Ticket", systemImage: "arrow.left.arrow.right", description: Text("Milestone placeholder"))
    }
}
